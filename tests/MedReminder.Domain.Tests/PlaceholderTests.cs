using Xunit;

namespace MedReminder.Domain.Tests;

// Sostituito nell'Incremento 1 dai test reali del dominio.
// Presente ora soltanto per rendere Incremento 0 verificabile con dotnet test.
public class PlaceholderTests
{
    [Fact]
    public void Solution_bootstrap_is_alive()
    {
        Assert.True(true);
    }
}
